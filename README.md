OpenMugen
=========

Open source King of Fighters code

Ubuntu
```shell
sudo apt install libsdl1.2-dev libsdl-image1.2-dev
```
Build
```shell
cmake -Bbuild
cmake --build build
```
Run
```shell
./build/OpenMugen
```